import React from "react";
import AppButton from "../AppButton";

export default function VoiceButton() {
  return (
    <AppButton label="Voice" onPress={() => undefined} variant="primary" />
  );
}
